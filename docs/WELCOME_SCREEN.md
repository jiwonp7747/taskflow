# TaskFlow Welcome Screen (온보딩)

## 개요

Source가 설정되지 않은 상태에서 "Failed to fetch" 에러 대신 친절한 온보딩 화면을 표시하여 사용자가 쉽게 첫 Source를 추가할 수 있도록 안내합니다.

## 문제 상황

### Before
- Source가 없으면 `/api/tasks`가 500 에러 반환
- "Failed to fetch tasks" 에러 메시지만 표시
- 사용자가 왜 에러가 발생했는지, 무엇을 해야 하는지 알 수 없음

### After
- Source가 없으면 `/api/tasks`가 `NO_SOURCE_CONFIGURED` 에러 코드와 함께 400 반환
- 친절한 Welcome Screen 표시
- Source 추가 폼 제공으로 즉시 해결 가능

## 구현 내용

### 1. API 에러 코드 개선

**파일**: `app/api/tasks/route.ts`

```typescript
// Source가 없으면 명확한 에러 코드 반환
const config = await loadConfig();
if (config.sources.length === 0) {
  return NextResponse.json(
    {
      error: {
        code: 'NO_SOURCE_CONFIGURED',
        message: 'No task source folder configured.',
      },
    },
    { status: 400 }
  );
}
```

### 2. useTasks 훅 확장

**파일**: `hooks/useTasks.ts`

```typescript
interface UseTasksReturn {
  // ... 기존 필드
  noSource: boolean;  // 추가
}

// NO_SOURCE_CONFIGURED 에러 감지
if (data.error?.code === 'NO_SOURCE_CONFIGURED') {
  setNoSource(true);
  setTasks([]);
  return;
}
```

### 3. WelcomeScreen 컴포넌트

**파일**: `components/onboarding/WelcomeScreen.tsx`

주요 기능:
- Source Name 입력 필드
- Folder Path 입력 필드
- "Create folder if it doesn't exist" 체크박스
- Add Source 버튼
- 에러 메시지 표시
- 로딩 상태 표시

### 4. 페이지 통합

**파일**: `app/page.tsx`

```typescript
// noSource일 때 Welcome Screen 표시
{noSource && !loading && (
  <WelcomeScreen
    onAddSource={addSource}
    onSourceAdded={handleSourceChange}
  />
)}
```

## 파일 구조

```
frontend/
├── components/
│   └── onboarding/
│       └── WelcomeScreen.tsx   # 신규
├── hooks/
│   └── useTasks.ts             # noSource 상태 추가
├── app/
│   ├── page.tsx                # Welcome Screen 통합
│   └── api/
│       ├── tasks/route.ts      # NO_SOURCE_CONFIGURED 에러 추가
│       └── config/route.ts     # createIfNotExist 처리 추가
└── types/
    └── config.ts               # AddSourceRequest 타입 확장
```

## 흐름도

```
┌─────────────────────────────────────────────────────────────────┐
│                        Page Load                                 │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                  GET /api/tasks                                  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
              ┌─────────────┴─────────────┐
              ▼                           ▼
┌─────────────────────────┐   ┌─────────────────────────┐
│   Source 있음           │   │   Source 없음           │
│   → 정상 응답           │   │   → NO_SOURCE_CONFIGURED│
└───────────┬─────────────┘   └───────────┬─────────────┘
            │                             │
            ▼                             ▼
┌─────────────────────────┐   ┌─────────────────────────┐
│   Kanban Board 표시     │   │   Welcome Screen 표시   │
└─────────────────────────┘   └───────────┬─────────────┘
                                          │
                                          ▼
                              ┌─────────────────────────┐
                              │   사용자가 Source 추가  │
                              └───────────┬─────────────┘
                                          │
                                          ▼
                              ┌─────────────────────────┐
                              │   Tasks 새로고침        │
                              │   → Kanban Board 표시   │
                              └─────────────────────────┘
```

## UI 디자인

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                     ╭────────────────╮                          │
│                     │   📂  →  ✓    │                          │
│                     ╰────────────────╯                          │
│                                                                 │
│                  Welcome to TaskFlow                            │
│                                                                 │
│          Manage your tasks with AI-powered markdown files.      │
│          Add a source folder to get started.                    │
│                                                                 │
│         ╭─────────────────────────────────────────────╮         │
│         │                                             │         │
│         │   📁 Source Name                            │         │
│         │   ┌─────────────────────────────────────┐   │         │
│         │   │ My Project                          │   │         │
│         │   └─────────────────────────────────────┘   │         │
│         │                                             │         │
│         │   📂 Tasks Folder Path                      │         │
│         │   ┌─────────────────────────────────────┐   │         │
│         │   │ /Users/you/project/tasks            │   │         │
│         │   └─────────────────────────────────────┘   │         │
│         │                                             │         │
│         │   ☑️ Create folder if it doesn't exist      │         │
│         │                                             │         │
│         │          ┌─────────────────────┐            │         │
│         │          │   ✨ Add Source     │            │         │
│         │          └─────────────────────┘            │         │
│         │                                             │         │
│         ╰─────────────────────────────────────────────╯         │
│                                                                 │
│         ┌─────────────────────────────────────────────┐         │
│         │  💡 What is a source?                       │         │
│         │  A source is a folder containing your       │         │
│         │  task markdown files (.md).                 │         │
│         └─────────────────────────────────────────────┘         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 사용 방법

1. TaskFlow를 처음 실행하거나 모든 Source를 삭제한 상태에서 접속
2. Welcome Screen이 표시됨
3. Source Name 입력 (예: "My Project")
4. Tasks Folder Path 입력 (예: "/Users/you/project/tasks")
5. 필요시 "Create folder if it doesn't exist" 체크
6. "Add Source" 클릭
7. Source가 추가되면 자동으로 Kanban Board로 전환

## 에러 처리

| 상황 | 에러 메시지 |
|------|------------|
| Source Name 미입력 | "Please fill in all fields" |
| Folder Path 미입력 | "Please fill in all fields" |
| 폴더 없음 + 체크 해제 | "Directory does not exist. Enable 'Create folder' option." |
| 폴더 생성 실패 | "Failed to create directory" |
| 기타 에러 | 서버에서 반환된 에러 메시지 |

## 관련 문서

- [SYNC_MECHANISM.md](./SYNC_MECHANISM.md) - 파일 동기화 메커니즘
- [FILTER_DATE_FEATURE.md](./FILTER_DATE_FEATURE.md) - 필터 및 날짜 기능
